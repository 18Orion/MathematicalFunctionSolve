This is a library and header for resolving a function from an input, given in a string.
You can use the library for whatever you want. The library is stored in include and in src.
For compiling the demonstration executable install clang and execute make.
